pub struct Characteristics {
    pub size: u8,
    pub dexterity: u8,
    pub strength: u8,
    pub constitution: u8,
    pub appearance: u8,
    // derived characteristics
    pub knockdown: u8,
    pub weapon_damage: u8,
    pub brawling_damage: u8,
    pub movement_rate: u8,
    pub major_wound: u8,
    pub healing_rate: u8,
    pub total_hit_points: u8,
    pub unconsciousness: u8,
}
