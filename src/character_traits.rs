pub struct ChasteLustful {
    pub chaste: u8,
    pub lustful: u8,
}

pub struct EnergerticLazy {
    pub energetic: u8,
    pub lazy: u8,
}
pub struct ForgivingVengeful {
    pub forgiving: u8,
    pub vengeful: u8,
}
pub struct GenerousSelfish {
    pub generous: u8,
    pub selfish: u8,
}
pub struct HonestDeceitful {
    pub honest: u8,
    pub deceitful: u8,
}
pub struct JustArbitrary {
    pub just: u8,
    pub arbitrary: u8,
}
pub struct MercifulCruel {
    pub merciful: u8,
    pub cruel: u8,
}
pub struct ModestProud {
    pub modest: u8,
    pub proud: u8,
}
pub struct PrudentReckless {
    pub prudent: u8,
    pub reckless: u8,
}
pub struct SpiritualWorldly {
    pub spiritual: u8,
    pub worldly: u8,
}
pub struct TemperateIndulgent {
    pub temperate: u8,
    pub indulgent: u8,
}
pub struct TrustingSuspicious {
    pub trusting: u8,
    pub suspicious: u8,
}
pub struct ValourousCowardly {
    pub valiant: u8,
    pub cowardly: u8,
}

pub struct CharacterTraits {
    pub ideal: String,
    pub chaste_lustful: ChasteLustful,
    pub energetic_lazy: EnergerticLazy,
    pub forgiving_vengeful: ForgivingVengeful,
    pub generous_selfish: GenerousSelfish,
    pub honest_deceitful: HonestDeceitful,
    pub just_arbitrary: JustArbitrary,
    pub merciful_cruel: MercifulCruel,
    pub modest_proud: ModestProud,
    pub prudent_reckless: PrudentReckless,
    pub spiritual_worldly: SpiritualWorldly,
    pub temperate_indulgent: TemperateIndulgent,
    pub trusting_suspicious: TrustingSuspicious,
    pub valourous_cowardly: ValourousCowardly,
    pub directed_trait1: String,
    pub directed_trait2: String,
}
